# js-collab-starter

A tiny inventory module used for the onboarding assessment.

- `src/inventory.js` — core module (legacy style).
- `src/bugs.js` — shared helpers.
- `test/run.js` — run with `node test/run.js`.
